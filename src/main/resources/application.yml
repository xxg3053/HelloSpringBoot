spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/HelloSpringBoot
    username: root
    password: kenfo0326
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true